1. Create an index on the company field in the stocks collection Creates a single field index on company to speed up search queries
db.stocks.createIndex({ company: 1 })

2. Create a compound index on the sector and sales fields in the stocks collection Creates a compound index to optimize queries filtering by sector and sorting/searching by sales
db.stocks.createIndex({ sector: 1, sales: 1 })

3. List all the indexes created on the stocks collection Displays all indexes present on the stocks collection
db.stocks.getIndexes()

4. Drop an existing index on the company field from the stocks collection Removes the index created on the company field
db.stocks.dropIndex({ company: 1 })

5. Use a cursor to retrieve and iterate over documents in the stocks collection Iterates through each document using a cursor and prints it
var cursor = db.stocks.find()
cursor.forEach(function(doc) {
   printjson(doc)
})

6. Limit the number of documents returned by a cursor to the first 3 documents Returns only first 3 documents from the collection
db.stocks.find().limit(3)

7. Sort the documents returned by a cursor in descending order based on the sales field Sorts documents by sales in descending order
db.stocks.find().sort({ sales: -1 })

8. Skip the first 2 documents in the result set and return the next documents Skips first 2 records and returns remaining documents
db.stocks.find().skip(2)

9. Convert the cursor to an array and return all documents from the stocks collection Converts cursor output into an array format
db.stocks.find().toArray()

10. Create a collection named "Companies" with schema validation Ensures each document must contain company and sector fields of string type
db.createCollection("Companies", {
   validator: {
      $jsonSchema: {
         bsonType: "object",
         required: ["company", "sector"],
         properties: {
            company: {
               bsonType: "string",
               description: "must be a string and is required"
            },
            sector: {
               bsonType: "string",
               description: "must be a string and is required"
            }
         }
      }
   }
})